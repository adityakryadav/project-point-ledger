# Partner Aggregation Service

## Features
- Fetch loyalty points from bank APIs (mocked)
- Convert points to INR
- ETL pipeline (extract, transform, normalize)
- Flask API endpoint: /fetch-points

## Run
python main.py --serve